# Copying files and folders from Linux to Windows server `using Ansible, Winrm, http and CredSSP`.
> Update: for personal use, update the `playbook.yml` and `hosts` files.

# Running:
```ansible
ansible-playbook -i hosts playbook.yml
```
